<?php

class PasswordController extends BaseController {

    /**
     * Used to make changes to the 'admin' password.
     */
    public function postReset() {

        // We should check that the current password matches first!

        // We need to make sure that the new passwords match!

        // We need to make sure that we save the new password!

        // We need to redirect the user back to the 'Settings' page and tell them it was successful!

        die('Done!');
    }

}

?>
